<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java1234.c05sundry.mappers.UserMapper">

    <resultMap type="User" id="UserResult">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
    </resultMap>

    <insert id="insertStudent" parameterType="User">
		insert into t_user values(null,#{name},#{age},#{pic},#{remark});
	</insert>

    <select id="getStudentById" parameterType="Integer" resultType="User">
		select * from t_user where id=#{id}
	</select>

    <select id="searchStudentsMultiParam" resultMap="UserResult">
		select * from t_user where name like #{param1} and age=#{param2}
	</select>
</mapper> 