<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.c02basicCrud.com.ims.mappers.StudentMapper">

    <resultMap type="Student02" id="StudentList">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
    </resultMap>

    <insert id="add" parameterType="Student02">
		insert into t_student values(null,#{name},#{age})
	</insert>

    <update id="update" parameterType="Student02">
		update t_student set name=#{name},age=#{age} where id=#{id}
	</update>

    <delete id="delete" parameterType="Integer">
		delete from t_student where id=#{id}
	</delete>

    <select id="findById" parameterType="Integer" resultType="Student02">
		select * from t_student where id=#{id}
	</select>

    <select id="find" resultMap="StudentList">
		select * from t_student
	</select>

</mapper> 